{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Nonogram App",
            "type": "debugpy",
            "request": "launch",
            "module": "app.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
